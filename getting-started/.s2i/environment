JAVA_APP_JAR=*-runner.jar

ARTIFACT_COPY_ARGS="-p -r lib/ $JAVA_APP_JAR"
# This ^^^ is required because without <uberJar>true we need all lib/** in /deployments/

JAVA_OPTIONS=-Dquarkus.http.host=0.0.0.0
